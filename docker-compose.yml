version: "3.8"
services:
  backend:
    build:
      context: ./backend
    image: crawl-app-backend
    command: ./local.sh
    volumes:
      - ./backend:/app
    ports:
      - 8000:8000
    links:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: crawldev
