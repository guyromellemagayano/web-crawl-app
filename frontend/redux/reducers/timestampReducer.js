// const counterReducer = (state = {value: 0}, action) => {
// 	return {...state};
// };

// export default counterReducer;

const timestampReducer = (state = {value: 0}, action) => {
	return {...state};
}

export default timestampReducer;